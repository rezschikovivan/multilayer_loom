[tool.ruff]

lint.select = ["E", "F", "I", "N", "UP", "B", "A"]
line-length = 150
lint.ignore = []

target-version = "py312"
lint.fixable = ["ALL"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages]
find = {packages = ["loom"]}